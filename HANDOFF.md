# Handoff — The Casino (Nic's Hoyle-style casino)

Give this file and the project zip to a fresh chat and it can pick up where the last one left off.

## What this is
A private, Hoyle Casino 1999–style casino for Nic's friends and family, played in landscape on iPhone/iPad as a web app.
Vanilla HTML/CSS/JS ES modules, no build step. One save on the device (`localStorage`, key `casino.save.v1`).
Nic designs all the art, voice lines and music himself and sends them in chat; the assistant integrates them.

## How it's tested and shipped

Build number: js/version.js (`VERSION = 'v128'`) shows at the foot of Settings (.build-tag) so a phone can be checked against the latest zip — bump it with every zip (build_sw.py prints it as a reminder). Nic wants it removed when the game is finished.

- Live test link (a multi-file Claude artifact): https://claude.ai/artifact/8cvLLqFKsWdH7V3HzkwN1Q
  Republish `artifact.html` **to that same URL** with a `files` map of the changed files (js/**, css/app.css, assets/**).
  The artifact tool refuses to overwrite files it hasn't seen in the session: read them from the artifact first, then publish.
  Nic does not want zip downloads for routine testing — the live link is the loop.
- Phone install: it's a PWA (manifest + `sw.js` generated by `tools/build_sw.py`, which must be re-run after any file change). Hosted on GitHub Pages from Nic's repo; Add to Home Screen in Safari; runs offline.
- Local: `python3 serve.py` then open the printed address on the phone (same Wi-Fi). `index.html` is the local entry.
- Tests: `npm test` (poker, blackjack incl. 1M-hand house-edge check, cribbage, farkle, luck, AI sims),
  `npm run test:ui` (Playwright play-throughs of hold'em and blackjack; screenshots land in tests/shots/).
  Playwright launches `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` in the cloud box; adjust if elsewhere.

## Status
Done: lobby (floor art with tap hotspots; open games carry NO text label — the painted signs do the job — labels only for "· soon", "· need money" and the Cashier; hotspots have data-game/title for tests), bank + 5 credit-card tiers (Basic → Silver $2.5k → Gold $5k → Platinum $25k → Sovereign $100k),
Ken's bailout ($1,000; friendly Ken photo first time, angry Ken after), Texas Hold'em (limit + no-limit, 5 tiers of tables),
Blackjack (6 decks, S17, 3:2, DAS, split to 4, split aces one card, insurance; 0–2 companions),
Cribbage (two-handed, first to 121, stake per game, skunk ×2 / double skunk ×3, loser deals next; hands counted with Nic's FIXED count panel (his cribbage-count-package, 2026-09-21; `.cb-show.cs` in showHand): never grows — left column = title, cards, ONE expression line ('7♦ + 8♣') + a note that replace themselves, progress dots; right column = COUNTING / 'Fifteen for 2' / 'Combination i' (NEVER 'i of n' and the dots appear one by one — Nic: showing how many are coming spoils the fun), big anchored total; 800 ms per combination, total ticks 200 ms in; when done the right becomes HAND TOTAL with a category summary (Fifteens ×2 4 / Pairs ×1 2 / Runs ×2 6) and 'Show breakdown' swaps the card area for the full two-column list; once the count is finished, tapping the count panel itself does the same as Next/Done — waitButton(label, cls, alsoTap)).
Farkle (6 dice, 1=100/5=50/three-of-a-kind, 4/5/6 of a kind = 1000/2000/3000, straight & three pairs 1500, two triplets 2500, 500 to get on the board, first to 10,000 with a final round; 1–3 opponents; ability = persona.farkleSkill ?? cribSkill; Nic's dice.mp3 on every roll). Farkle look comes from Nic's mockup: the cleaned mockup is the full-screen background (assets/img/table/felt-farkle.jpg, stretched 100%/100%), serif gold UI, dice cup (assets/img/farkle/cup.png) shakes then tips, dice scatter with random tilt inside ZONE in farkleTable.js.
Slots (2026-09-21, Nic's van-halen-slot-package): "Van Halen · Hot for Jackpot", one machine (SLOT_TABLES: bets 1/2/5/10/25/50/100, bring $20–$2,000, tier 1). js/core/slots.js = 3 reel strips × 24 stops, 6 symbols (0 VH, 1 record, 2 amp, 3 seven, 4 bolt, 5 guitar), PAYTABLE as bet multiples (3×VH = JACKPOT 25× since v122 (15× in v121, 200× before); guitars 50, sevens 25, amps 15, records 10, bolts 5, two VH 3, one VH 1, two alike 0.3) → reels alone RTP ~82%, 68% of spins pay something (tests/slots.test.mjs). LUCK.slotSave 6%: a losing spin becomes a small win (never the jackpot). LUCK.slotJackpot 5% (v121, Nic: 'it's a game, not real life'): one spin in twenty is forced to three logos — pull() rigs the auto stops and a hand STOP on a rigged spin brakes at the logo's next pass (spin.rigged → planAuto) so it lands; overall payback ≈ 200%, i.e. the slots are a money fountain by design. Natural three-logo lines (1 in 1,728) pay the same 25×. js/ui/slotsTable.js draws his cabinet (assets/img/slots/background.jpg) + reels/handle/LEDs on a 1774×887 canvas stretched to the screen, symbols cropped out of symbols.jpg (TILES), invisible .slot-hit buttons over the painted ones (paytable, sound→settings, −/+, SPIN, handle) plus the normal '‹ Leave table' .leave-btn top-left. background.jpg is Nic's cabinet with his painted 'BACK TO CASINO' button and 'Nic' chip painted out (the original is his package's background; he asked for both to go). STOP (v82–v86): the painted SPIN is covered by a canvas-drawn STOP (ellipse in the button's cream + text at 1466,771; red with a glow while a reel can be braked, dim brown otherwise; does nothing when idle — spins start from the handle only). v86 made it a real skill-stop: reels spin up over ACCEL 0.35 s to V = 12 rows/s and cruise; brake(i, tp, BRAKE_ROWS 2.5) eases the reel from wherever it is onto the whole row 2–3 rows on (D = 2·d2/V ≈ 0.4 s), so a manual stop lands on whatever the player's timing gives — the result is evaluateStops() over the actual stops at settle. Reels left alone brake at a planned autoAt (the first moment ≥ AUTO_STOP[i] − 1 s at which the machine's own target is exactly 2.5 rows ahead; AUTO_STOP = 2.8/3.6/4.4) so the machine's pick — and the LUCK.slotSave — only applies to reels the player didn't stop. After a manual STOP the reels still rolling are re-planned (planAuto) to wait at least STOP_GRACE 1.6 s (+0.8 s for each further reel) from that press before stopping on their own (v87, Nic: 'after pressing stop once increase the timer a little'). spin.endAt/resultAt follow the last reel; settle() fires from draw(). Timing is a uniform sample of the strip, so the long-run RTP is the same 92.6% whether you stop or not — the fun is the trying. Each effective press plays slotstop.mp3 (Nic's button_press.mp3 at 1.35×). Sounds (v81): slotspin.mp3 = Hot for Teacher drum intro from 1.1 s, 4.65 s long (ends right where the band would hit — about when the last reel auto-stops; it is pause()d the moment the last reel lands, so an early STOP cuts it short); slotreels.mp3 = a 1.4 s whir looped (element.loop) under every spin, cut with the drum roll when the last reel lands — it is slotmiss.mp3 1.3–2.7 s; his package cues slotwin/slotmiss are no longer played (their fixed-time stop ticks were the 'ts ts' Nic disliked); slotclunk.mp3 exists but is unused since v85 (Nic: 'what is that ts ts sound? Remove it') — a spin that pays nothing is silent after the reels stop; slotsmall.mp3 = Nic's moneyUP.mp3, played at settle for the partial 'two alike' payouts (30% back — 'a couple dollar wins'); slotteacher.mp3 = song 70.9–76.4 s ('got it bad ×3, I'm hot for teacher'; the crash is at 69.4 s, vocal start located by envelope, not by ear) plays on any payout ≥ the bet (28% of spins; the 30%-back 'two alike' partials still get the miss clunk); any clip still playing is cut by the next pull (stopClips()); slotjackpot.mp3 = Jump: synth intro 0–7.4 s with a 0.35 s equal-power overlap into 74.5–88 s — Nic picked 1:14.5 by ear as the start of 'Ah, might as well jump' (the 56.4 s guess was the wrong spot); loudnorm I=-14; the JACKPOT banner waits 6.9 s so it lands on the vocal. Jackpot: Jump plays, 6.9 s later a resultBanner 'Might as well jump!' ★ JACKPOT ★; wins ≥10× get a 'Big win' banner. Bust → 'Buy more credits' modal (¼ of maxBuy). Sound playback (v129 — fixes Nic's 'after a minute on the phone it gets laggy and sounds stop'): every effect and voice line plays from a decoded AudioBuffer (bufferFor() fetch+decodeAudioData once per file; effects are all decoded on the first tap, voice lines on first use) through buffer → gain → speakers, and the handle returned by audio.play()/playClip() has pause()/paused/ended/loop (audio.play(key, { loop: true }) for the reel whir). v111–v128 wired a new <audio> element + MediaElementSource into the graph per sound and never released them — iPhone Safari caps live media elements, so it slowed down and then stopped starting sounds; desktop browsers never showed it. Volumes (v111): settings.musicVolume 0–1.5 (0.7 = standard, a datalist tick marks it; above 1 is a WebAudio boost — songs are -31 LUFS so there is headroom), settings.sfxVolume and settings.voiceVolume 0–1 (sliders under their checkboxes in Settings). All clips (sfx files and voice lines) now play through playClip(): element → gain → speakers (iPhone ignores element.volume), falling back to a bare element before the first tap; the synth placeholders share an sfxBus gain. v114: the faint rounded 'ghost' box around the hold'em TOTAL POT on phones was .pot-plaque::before (inset 3px, 1px border) stretching to the .under-board because the plaque was position: static in plaque-below mode — now position: relative with left/top: auto (v115 — plain relative inherited the base rule's left:50%/top:47% and shoved the plaque right). Blackjack's ♫/⚙ live at the TOP right on the rail (top 24% / +gear+10px) at every size, since the bottom-right corner is a player's portrait; on phones (max-height 460px) the side players' nameplates sit ABOVE their circles while the betting panel is up (v115: :has(.actionbar.betting) rule, top:auto; bottom: 3.4vw+22px — the base rule's `top` had to be cleared) and under them during play; .bj-seat also carries .left/.right; the hold'em hero seat is x 39% on phones (plaque-below) so the hole cards clear the tray. Exit (v116–117): Settings' button row is split — '⇥ Exit Game' bottom-left (dark emerald, muted gold border, ivory text, door icon) and gold 'Done' bottom-right; confirm modal first (Nic wanted 'Exit Game', not 'Return to Title'). exitGame() in app.js (lobby.js dispatches 'casino:exit'): snapshot chips (bank.setAtTable), stop clips + music, window.close() — Android home-screen apps usually do close; iPhone/iPad ignore it, so 350 ms later, if the page is still visible, a full-screen #exit-screen 'Closed for the night' card shows (bank incl. table chips, 'swipe home now', 'Come back in' = reload). Away timer: visibilitychange hidden → snapshot; back after > AWAY_LIMIT (60 s) → reload to the intro with the bank restored (skipped while the exit screen is up). The 'Art files: N still placeholders' report is gone from Settings (assets.report() in the console still lists them). Updates (v113): app.js listens for serviceWorker controllerchange — if a new build takes over while the intro is still up the page reloads once so the new version shows on that launch (before, a deploy showed up only on the SECOND launch because the old cache served the first); mid-game it just toasts. Phone height (v112–113, @media max-height 460px; the hold'em tray also tightens so it clears the hole cards with the iPhone side inset): the blackjack betting panel shrinks (chips 34–44 px, smaller Deal) so the side nameplates stay visible; farkle's action row tightens and is capped in width, and .fk-msg sits above the safe-area inset so it clears the buttons on notch iPhones. Intro (v110): the faint 'WARBAND GAMES' watermark pressed into the felt (in the video and the splash poster) is painted out by tools/clean_intro.py — frame 0 is cleaned with a Poisson clone of the felt above it, every frame takes the clean felt where it still matches frame 0 and its own pixels where something moves over the box (cards, the logo); re-encoded x264 crf 19, audio copied; splash.jpg = cleaned frame 0. Cribbage board (v106): Nic's new walnut-and-emerald board (2206×713 PNG) had ~48 holes a row in groups of 4, so tools/drill_board.py repaints the track bands with the lane's own texture, drills 60 holes a row (12 groups of 5) from a hole sprite, redraws the 0/15/30/45/60 and 120/105/90/75/60 markers, crops to the board and squeezes it to 85% height (2134×302) so it still sits in the scene's rail; BOARD_ART (holeX/rowY/start[lane][0..1]/plate/window) came from the script's printout, holePos uses the two side-by-side START holes (s<0 → start[1], s=0 → start[0], 121 → start[0]). Both aspect-ratio rules are 2134/302; the taller board pushed .cb-opp-hand to 33%, .cb-center to 52%, the starter to 30%. Pegs: his gold/red peg PNGs; score windows gold (#f0d27a) for you and ruby (#e8474f) for them to match. If Nic regenerates the board at ~7.8:1 (say 2200×285) with 60 holes a row, drop the squeeze (SQUEEZE=1) and re-measure. Hold'em tournaments (v122, replaces Fixed Limit — Nic: 'get rid of limit'): HOLDEM_TABLES mode 'tourney' = The Open $200 / The Classic $1,000 / The Major $5,000 / High Roller $20,000 / Championship $100,000 (tiers 1–5; named by Nic in v128 — they were 'Sit & Go $200' etc.; the ids stay sng-200 … sng-100000 because saved statements reference them) with prizes [4×, 2×] the entry (1st $800 / 2nd $400 at the $200 table; v123 — the 12.5×/5× first cut skipped card tiers), 1,500 tournament chips each, TOURNEY_BLINDS 10/20 → 2000/4000 rising every TOURNEY_LEVEL_HANDS = 8 hands PLUS one level per player knocked out (levelBoost, v124 — Nic: 'when someone gets removed make the blinds go up slightly') (toast + topbar title 'Sit & Go $200 · Blinds 25/50'), minimum 2 opponents (minOppFor in holdemSelect; tableSelect hides the buy-in slider when minBuy === maxBuy and shows buyInNote). holdemTable: this.tourney, this.game = { ...table, mode:'nolimit', sb, bb } is what Hand/AI see (blinds mutate per level), this.fmt = fmtChips (plain numbers, exported) for stacks/pots/bets, bank.buyIn(entry) at sit-down, setAtTable stack 0 always (chips aren't money — app.js snapshot also passes 0), recordHand pot/net 0. Busted opponents: seat.out = true (nameplate says OUT, .seat.out dimmed), no rebuy/replacement. betweenHands: human busted → place = players still holding chips + 1; last one standing → 1st; prizes[place-1] (0 for 3rd+); .tourney-result modal ('You won the tournament!' with the big winBanner / 'Nth place — in the money' / 'Out in Nth'), then leave() cashes out this.payout only (leaving early forfeits the entry — askLeave says so). bank.cashOut skips the statement line when the amount is 0. tests/sng.smoke.mjs plays one to a finish (in npm run test:ui). The engine's limit code still exists, just no tables use it. Champion ceremony (v127, Nic's championship-clap-package + victory.mp3 + one_man_clap.wav): winning ANY Sit & Go (place 1) runs js/ui/championship.js instead of the old bigwin banner + modal — a fixed full-screen .champ overlay (canvas + DOM plaque, z 45): music ducked, sfx 'victory' (his victory.mp3, 6.7 s) from t=0, the table dims 0.6–1.45 s, 64 gold/emerald chips rain 1.75–5.25 s, the plaque (trophy.webp cut from the package, TOURNAMENT CHAMPION / name / '1st of N players' / PRIZE / table name / gold Collect) rises in 1.35–2.15 s, and the applauding guest (assets/img/table/applause.webp = his applause-poses.png 2×2 sheet at 1000², lossy webp with alpha) fades up at 1.55 s to the LEFT of the plaque (size = min(plaque height ×1.05, room between the safe inset and the plaque, 300 px) so he never sits behind it; the plaque is 38vw ≤ 440 px for that reason). Head and shoulders are one still frame; only the lower half cross-fades through poses 0→1→2→3→2→1 every 0.8 s from 2.15 s, so hands meet at 2.55 / 3.35 / 4.15 / 4.95 — each plays sfx 'clap' (one clap cut from one_man_clap.wav at 1.550 s; the file had eight), then he holds pose 3. A sparkle burst around the trophy 1.72–3.3 s; prefers-reduced-motion = still pose, no chips. Collect (any time) → leave() cashes the prize. tests/champ.smoke.mjs runs the ceremony directly over a table at phone + iPad sizes (shots champ-*.png); sng.smoke handles .champ when the bot happens to win. `TEST_CHAMPION` at the top of holdemTable.js (false in every real build) makes one won hand end the tournament in your favour so the ceremony can be checked on a phone in a minute. v153: count.mp3 = Nic's beep.mp3 (0.065–0.5 s, +4 dB), played at its own pitch for every combination — replaces the v152 rendered ping and its climbing scale. v152 (superseded): count.mp3 was a brighter rendered ping (E6 with two decaying harmonics, ~0.2 s, peak -5 dB) played at full volume, and each combination steps up a major scale (rate = 2^(semitone/12), 0-2-4-5-7-9-11-12) so a big hand climbs an octave — Nic: 'a fun beep, counting up'. (v151 had the old quiet synth chip bounce.) v150: cribbage discards play crib1.mp3 then crib2.mp3 380 ms apart (two tosses cut from Nic's crib2.mp3 at 1.04 s and 1.42 s, peaks ≈ -3 dB) for you and the opponent — v149's fold tosses lasted one build; he wanted the crib to sound different from a hold'em fold. Send to crib has no sound of its own. v148: the cribbage count panel (hand / crib) opens silently — the 'flip' it played on every show is gone; the starter cut still flips. v147: the v146 deal change (trimmed deal.mp3 lead-in + 200 ms per card) was reverted at Nic's request — deal.mp3 is the original again and the pace is 130 ms (hold'em) / 90 ms (cribbage). v145: every cribbage combination, yours or theirs, ticks with 'chip' (v144 gave yours the rising pot-chips; Nic preferred them the same). audio.play still takes opts.rate → playbackRate if anything wants it. v143: cribbage and blackjack no longer play a shuffle at sit-down (cribbage's hand shuffle followed 300 ms later — two back to back with the deal over the top); cribbage shuffles once per hand and waits 1.75 s before dealing, blackjack's fresh-shoe shuffle waits 1.75 s too. Same rule as hold'em: one shuffle, then the cards. v142 (Nic, twice: 'the sounds are all the old ones' right after installing a build): sound files were discovered at start-up by probing every key with an <audio> element (6 s timeout each); on an iPhone that's still downloading the new build's cache the probes time out and every effect falls back to its synth placeholder for that session. Now tools/build_sw.py writes assets/manifest.json ({sfx:[files], music:[files]}) and audio.init()/music.init() read it — no probes unless the manifest is missing; and audio.play() tries `sfx/<key>.mp3` once for any key the manifest didn't list before settling for the synth (missingSfx). RUN build_sw.py after adding any sound, or the game won't know it's there. v141: 'yourturn' only chimes when the turn has just come round to you — blackjack tracks lastActor per round (no chime after each hit, or on the second split hand), cribbage pegging tracks the last turn (no chime when you play again after a go). v140: shuffle eased back (riffle at 1.12×, 120/220 ms gaps, ~1.5 s) and the deal waits 1.75 s; after the human folds every AI action takes at least 1 s (thinkTime floor) and the street pause is 600 ms — Nic felt the rest of the hand went by 'all at once'. v139: shuffle.mp3 = Nic's shuf.mp3 tightened to ~1.2 s — riffle 2.12–2.72 s at 1.3× tempo, 70 ms gap, bridge 2.97–3.38 s, 120 ms gap, tap 3.87–4.12 s (ffmpeg concat, -22 LUFS); hold'em waits 1.15 s after starting it so the first card follows the tap with no overlap (v138 used the raw 2.05 s cut and the deal ran over its tail); the extra shuffle that run() played at sit-down is gone (he heard two before the first hand). Blackjack's shoe and cribbage use the same clip. v137: the v136 'every button taps' was too much — the old synth tap had been nearly silent, and Nic's click at full level on every press (it had always been on the hold'em action buttons too, under their own sounds) made 'all the sounds seem off'. Now: tap plays at TAP_LEVEL 0.3 by default (audio.js), the hold'em fold/check/call/raise buttons and the raise confirm no longer tap at all (their own sound is immediate), and app.js's document listener only taps .btn buttons that aren't .act and .cast-tile — never anything inside .actionbar or the slot machine. v136: one document-level click listener in app.js plays 'tap'; audio.play() drops a tap that comes within 80 ms of any other effect (so a button that plays its own chip/raise/slotstop, or one that already called play('tap') itself, doesn't double up). chip.mp3 is now Nic's pick, option A (poker-chips-36816 at 0.905 s, 0.16 s). v135: tap.mp3 = Nic's click (72 ms, every button in the game); yourturn.mp3 = his urturn clip trimmed to the double tick (0.12–0.62 s, -22 LUFS); chip.mp3 = one chip clack cut from freesound poker-chips-36816 at 1.405 s (0.17 s) — the single-chip tick for blackjack bet chips / insurance / slot bet / small cribbage scores. Only bailout is still synth. v133: win.mp3 / bigwin.mp3 are Nic's files cut to the hit (win 0–1.6 s, bigwin 0–1.9 s, fades from 1.1 / 1.3 s (v134 — the v133 cuts were shorter and Nic wanted more), -20 / -17 LUFS — he didn't want the long tails); every game shares those keys. tierdown → ALIAS to lose's file (the sadder synth lasted one build). Sound pass (v132, Nic listened to the whole hold'em set from tools/render_synth.mjs — it bounces the synth recipes through an OfflineAudioContext in headless Chromium, exact copies of what the game plays): keep the synth win / bigwin / tierdown (tierdown now sadder: three chromatic steps down and a long drooping note with a low sine under it); fold.mp3 = the first toss from his fold file (0.30–0.75 s, -24 LUFS); shuffle.mp3 = the first riffle of his shuffle file (0.22–1.32 s, -22 LUFS) and hold'em now plays it before every deal (650 ms before the first card; blackjack/cribbage already used the key); ALIAS in audio.js: chips → allin's file, flip → deal's file (no files of their own). tierup.mp3 (v131) = Nic's level_up.mp3 (loudnorm -17) — plays when a cash-out lifts the card tier, every game. tierdown is still the synth. Hold'em loss sound (v130): 'lose' only plays when you were still in the hand and lost — folding (blinds or a bet you let go of) is silent, cash games and tournaments alike (Nic: 'if the player folds it shouldn't make a losing sound'). Table talk (v126): a character who spoke less than 2.2 s ago holds the next line (seatEl.spokeAt in every table's talk(); Nic saw Courtney's 'Nice' and 'can't take the heat' land on top of each other), and audio.voice() skips a clip while another voice clip is still playing (voiceNow). v171 FARKLE LUCK (Nic: 'I don't win that often either' — farkle was a straight roll): each FARKLE_TABLES entry has `save` = how many of YOUR farkles in 100 are saved — $10 50, $50 45, $250 40, $1,000 35, $5,000 30. Game.roll() calls game.luckyRoll(game) when a roll has nothing in it; farkleTable sets it to luck.js farkleSave(), which (on your turn only) re-rolls the same number of dice until something modest scores (best keep ≤ 400 — a 1, a 5, a small set, never a big combination), so the turn carries on. Opponents farkle normally. tests/fk.sim.mjs [games] [humanSkill] [save|table id] [opponents 1|3]. Measured at $10 (50): casual (0.2) vs 3 random opponents 18% → 39% (fair share 25%), average (0.5) 24% → 49%; one-on-one casual beats Kurtis 76%, Nic 66%, Mom 56%, Vee 51%. At $5,000 (30): casual vs 3 = 28%, average 36%. With three opponents even a fair game is 1 in 4, which is most of why farkle felt hard.
 v170 CRIBBAGE LUCK (Nic: 'I have never won a game of cribbage' — until now cribbage was a straight deal): each CRIBBAGE_TABLES entry has `nudge` = hands in 100 that go your way — $10 40, $50 35, $250 30, $1,000 25, $5,000 20. luck.js cribNudge(rng, per100) picks the kind by LUCK.cribHandShare (0.5): 'hand' → Game.deal(arrange) deals from luckyCribDeck(), whose six for you contain a keep averaging ≥ LUCK.cribHandMinEV (9) points before the cut (you still have to throw the right two); 'cut' → game.beforeCut = luckyStarter(), which moves a starter from the better half of the cards that help you (hand, crib if yours, his heels) more than the opponent onto the top of the deck. tests/crib.sim.mjs [games] [humanSkill] [nudge|table id] prints your win rate against every character (bot on the same discard/peg AI). Measured, casual player (skill 0.2) → no luck / 40: Kurtis 57→72%, Freddy 49→72%, Nic 29→52%, Mom 16→32%, Vee 12→29%; average player (0.4) at 40: Nic 66%, Mom 44%, Vee 44%. Mom (0.85), Vee (0.95) and Dot (0.75) stay the sharks.
 v169 HAND RANKINGS: a round gold '?' (.help-btn) sits in the hold'em topbar beside the bank (cash and tournaments) and opens js/ui/handRankings.js showHandRankings() — a modal listing the ten hands best→worst in two columns (1–5 left, 6–10 right), each with its name, a plain one-line note and five example cards (HAND_RANKS, parseCards + cardEl, local --card-h/--card-w: 35/25 px on phones, 50/35 on tablets), a line on top ('your best five cards…') and a foot line on how the kicker breaks ties. Once the flop is out and you're still in the hand, your current hand's row is gold with 'You have this' (holdemTable.currentHand(); a royal flush is category 8 with describe() 'Royal Flush', mapped to row 1). Fits an 844×390 phone without scrolling. tests/ranks.smoke.mjs (in test:ui) opens and closes it at phone + iPad sizes.
 v168: farkle.mp3 = Nic's wahwah.mp3 from 0.27 s, sped up 2.1× with pitch kept (atempo), faded out, -18 LUFS, 2.3 s — plays on every farkle (yours or theirs) instead of 'lose'; ALIAS farkle → lose if the file is ever missing. v167 RESUME AT EVERY MONEY TABLE (hold'em cash, blackjack, cribbage, farkle): bank.state.atTable now carries { game, tableId, stack, opponents, savedAt, state } written by each table's checkpoint() at safe points — hold'em: before every deal and after every hand (button, handNo, opponents' stacks and tilt, humanStats); blackjack: at the start of every betting phase (so a rebuy counts) and after each round is paid (every seat's stack, lastBet — a round cut off mid-way is not played, bets come back); cribbage: before every deal and after every counted hand (scores, pegHistory, dealer, handNo → Game rebuilt so deal() moves the crib on as usual) and after each game is paid; farkle: at the start of every turn (scores, onBoard, current, finalRound, closer, pending, turnsTaken) and after each game. Stakes in cribbage/farkle are only settled at game end, so nothing is lost mid-game. bank.load() leaves a resumable atTable alone (old rule — chips back to the bank — still applies to slots and old saves); app.offerTableResume() on launch: 'Pick up where you left off?' (table, game, chips, cribbage score) → Continue (resumeTable: new XTable(…, { resume: snap }), no buy-in taken, no greetings) or Cash out (bank.settleAtTable → cashOut, toast, tier-change toast). app.snapshot() on pagehide no longer overwrites a resumable save. A tournament offer still follows on the next launch if one is saved too. tests/tableresume.smoke.mjs. v165 OFFLINE MUSIC: songs still stream from the network the first time and stay out of the service worker's app cache, but audio.js now keeps each one in a separate Cache Storage cache 'casino-music' after it plays once (saveSong), and Casino Radio has a 'Save all songs' row (music.saveAll, shows 'N of 25 songs saved for offline' / 'All 25 songs saved on this device'). A saved song plays from a blob: URL made from the cached file (songSource) — no range requests through the service worker, which iOS media is picky about. Offline (navigator.onLine false) the shuffle only picks saved songs; an unreachable song skips to the next. sw.js's activate step keeps 'casino-music' when it clears old caches, so saved songs survive every update. navigator.storage.persist() is requested so the browser doesn't evict them. The track node carries .file (the song file name) since el.src can be a blob now. tests/offline.smoke.mjs: save all → go offline → reload → the game opens and a song plays. v163 (polish pass + tournament saves): TOURNAMENT SAVE — bank.state.tourney = { tableId, handNo, button, level, levelBoost, seats:[{id, stack, out, tilt}], humanStats }, written by HoldemTable.saveTourney() just before each hand is dealt (so a closed game replays that hand from the top with pre-hand stacks) and cleared when the result is decided (betweenHands) or in leave() (forfeit). HoldemTable(…, { resume: snap }) → restoreTourney() and no entry fee; run() says 'Welcome back…'. Leave table in a tournament opens requestLeaveTourney(): Forfeit (confirm) / Keep playing / Save & leave (saveAndLeave → lobby, save kept). On launch app.offerResume() shows 'Your tournament is waiting' (Later / Continue); the Hold'em room shows that tournament's tile as '.table-tile.saved' with 'In progress · chips · players left' and 'Continue ›' (tableSelect opts.saved/savedNote/onResume/beforeSit); starting a different tournament asks to give the saved one up. Exit Game in a tournament says it's saved. tests/resume.smoke.mjs covers reload-mid-hand → Continue, Save & leave → Continue tile, Forfeit. MOTION — a new screen fades up from the dark background (screen-in, .32 s); the old one is removed at once (v164: the v163 crossfade left the old menus ghosting over the new screen — Nic); dialogs spring in (--ease-spring) and drop out fast; buttons/.act/tiles press in and brighten on hover; select-screen tiles stagger in; dealt cards toss in (deal-in, fill-mode backwards so .hl transforms still work); components.countTo() rolls stacks, pots-won and the bank figure (hold'em, blackjack, cribbage, farkle) — all off under prefers-reduced-motion. CONSISTENCY — tests/gallery.mjs screenshots every screen and dialog at phone (notch insets) and iPad sizes into tests/shots/gallery; fixes from it: one-group rooms (blackjack/cribbage/farkle/slots) lay tables out in two full-width columns (a lone slots tile centres); Settings fits a landscape phone without scrolling (labels beside controls, Casino Radio + Save data on one line, max-height 460px rule); Settings labels in Georgia like the checkboxes; cribbage said 'Nic deal first.' for the human — now 'You deal first.' / 'You cut…'; leaving cribbage before the first Deal did nothing (the Deal button moved mid-felt in v104) — fixed; slots sit text no longer mentions a SPIN button. LOADING — index.html paints #0b1a12 before the stylesheet and preloads splash.jpg (no white flash on a cold start). CLEANUP — slotwin/slotmiss/slotclunk (never played since v85) removed from SFX, the synth table and assets/sfx. v162: 'Be smart.' only (no name — Nic will record it once). holdemTable.sympathy(): when you lose a showdown after putting in ≥ 25 big blinds and the Nic character is seated (not out), 70% he says 'That sucks.' / 'Brutal.' (lines.brutal); if Freddy is seated too, half the time it's instead Nic 'You hate to see it.' (hateToSee) then Freddy 1.7 s later 'You really do.' (reallyDo). Waits for Nic to finish anything he just said. v161: Nic's inside joke — holdemTable.maybeBeSmart(): when the Nic character bets or raises heads-up against the human and his hand will win (Hand.finalBoard() reads the rest of the board off the deck), 50% of the time he says 'Be smart.' / 'Be smart, {player}.' (lines.beSmart), once per hand, instead of his normal raise line. v160: showdown text names the kicker when it decided it — evaluator describeVs(win, beatenScores) appends ', 9 kicker' when a beaten hand has the same describe() name (SHOWN = ranks describe already names per category); poker.js adds award.told (single winner only; a split never needs one) and holdemTable's 'X wins N with …' line and the lose banner use told || hand. v159: tournament nudges 25 / 20 / 15 / 10 / 5 (The Open → Championship; Nic); all five no-limit tables 25. v158: holdemNudge reads the kind-mix from LUCK.holdemFamily/holdemValue/goodHoleCards (one source; the rate lives on the table, the mix in luck.js). v157: tournament nudges set by Nic to 15 / 10 / 7 / 4 / 2 (The Open → Championship); cash tables stay 25. Hold'em luck v156 (Nic: 'its own card help, not a % on a %'): every hold'em table in HOLDEM_TABLES has `nudge` = hands in 100 dealt in your favour — cash tables 25, The Open 10, The Classic 7.5, The Major 5, High Roller 3.25, Championship 1.25 (same effective rates as v155). luck.js holdemNudge(rng, per100) rolls it and picks the kind with the 3/8/14 family/value/hole mix; the old per-tournament `luck` multiplier is gone. tests/sng.sim.mjs 5th arg is now a nudge (hands per 100) or a table id. Hold'em luck v155: goodHoleCards 0.12 → 0.14 so cash tables nudge a round 25 hands in 100 (3 family + 8 value + 14 better hole cards; Nic wanted the math easy). Tournament rates are then 40% → 10 in 100, 30% → 7.5, 20% → 5, 13% → 3.25, 5% → 1.25. Tournament luck v154 (Nic: 'add 5% to each'): 0.40 / 0.30 / 0.20 / 0.13 / 0.05 — the Championship is no longer a fully honest deal. Tournament luck (v125): each tourney table has `luck` = scale on the hold'em luck system (cash tables stay at 1): $200 0.35, $1,000 0.25, $5,000 0.15, $20,000 0.08, Championship 0 (honest deal). luckyDeck() divides the roll by it. Measured with tests/sng.sim.mjs (args: tournaments, opponents, weak|average|good|shark, luck-scale-or-table-id) — an average player vs 5 of the family: 1st 33% / paid 50% at $200 down to 1st 23% / paid 40% at the Championship (fair share 17%); full cash-game luck was 58% first, which is why it's scaled. Cribbage (v119): 'Show breakdown' sits centred in the count panel's foot row (grid 1fr auto 1fr; .cs-foot-text on the left) — the YOUR POINTS pop covers it for its 1.9 s then it shows; your own hand/crib total plays 'bigwin' at ≥12, 'win' at ≥6, else a chip tick, with the points pop. Cribbage polish (v104): starter and crib share one column (right 7%, width 14%, both cards at .8× so they match; label gap 6 px each); the bottom status .cb-msg is lifted 14 px off the edge with a full frame; the mid-felt Deal is smaller and while it shows the table carries .deal-ready so the status line ('Kurtis deals first.') sits directly under the button. v105: opponent's .cb-points.opp is emerald with a gold frame and a small red peg dot before the label (solid red read as a penalty); the board plates / lane labels say 'You' for the human (boardName()) since an opponent can also be called Nic. All five games are in. Checkboxes app-wide are custom (appearance:none) green tiles with a gold frame and gold tick (v93, from Nic's Music Library mockup). App icon (2026-09-22): Nic's chip-and-cards 'W' art → assets/img/icon-180/192/512.png (512 is 'any maskable'; the felt bleeds to the edge so iOS's rounded mask looks right); index.html apple-touch-icon + manifest icons. iOS only refreshes the icon when the app is re-added to the Home Screen.
Next: polish / whatever Nic finds while testing.

## Rules Nic set (don't break these)
- Accessibility for his 65-year-old dad: big cards, big cutout portraits (no circles), big chips with numbers, big result tags and hand names, big bank card.
- Don't deal the next hand until the player presses Deal. Fun win screen with a chip stack; BIG WIN = net gain ≥ 25 BB (hold'em) or blackjack / ≥ 8× min bet (blackjack).
- Character picker: real people first by default; real people show name only (bots keep taglines). Bots: marty, dot, rico, vee.
- Cast personalities: Nic = best, tight, only calls with a good hand. Freddy = aggressive, bluffs, shoves. Kurtis = terrible, calls with nothing, never aggressive.
  Nathaniel = conservative, never bluffs, calls with any pair "within reason". Mom = between Nathaniel and Freddy, jolly, says "Damnit." on a big loss / bad beat. Courtney = skill 4/10, calls too much (persona.station 1.6); first character with a full recorded voice set (assets/voice/courtney/*.mp3). Ken = banker.
- Hold'em feel (Nic, 2026-09-20): a family game, not a card room. ai.js has FAMILY_SKILL 0.8 (everyone plays below their dial) and LIMP_URGE 0.2 (+ (1-tight)*0.4): when nobody has raised, most people call the blind to see a flop (Nic ~40% VPIP). Freddy has persona.checkRaiser 1: whenever it checks to him postflop he bets, with the `tooMuchChecking` line.
- Cribbage ability is a separate dial, persona.cribSkill: Mom .85 (the family shark), Nic .45, Nathaniel .45, Freddy .2, Courtney .2, Kurtis .12; bots use their poker skill.
- Leaving: 'Leave table' always works. Mid-hand/mid-game it asks Stay / finish first / Leave now (askLeave in components.js). Leave now = fold (hold'em, pot stays), forfeit the bet (blackjack) or the stake (cribbage, farkle). Nic: nobody should ever have to close the app to get out.
- Luck (Nic, 2026-09-21; js/core/luck.js): sporadic, NOT tied to session start/return — Nic explicitly did not want "welcome back" reinforcement loops. Hold'em, per hand: 3% "family" deck (everyone has something, big pot, human best at river), 8% "value" deck (human best at river, 1–2 opponents hold real second-best hands so they call/raise legitimately), 12% just better hole cards (everything else honest). Rigged hands need human preflop strength .28–.8, ≥ pair on the flop, ≥ two pair at the river, strictly best; they only pay off if he stays in. Blackjack: 6% of deals make 20/21, 15% of hits on 12–16 land 17–21, 15% of dealer draws bust while he stands on 12–16. Everything else is a straight deal so he still loses from normal play. Constants in LUCK; tests/luck.test.mjs. The human is `HUMAN` ('you') — luck never applies to companions/bots.
- Hold'em pot (Nic, 2026-09-21, his total-pot-animation.html): PotPlaque in holdemTable.js — 'TOTAL POT' plate, serif, gold rim, amount counts up over 320 ms ease-out and the rim pulses gold when the pot grows; hidden when empty, hidden after payout. Tall screens (innerHeight ≥ 560: iPad/desktop) float it centred above the community cards; phones (`.plaque-below`) put it in the under-board row (left 52.5%, 27vw wide, wraps) beside the pot chips, right of your hole cards (the top seat covers the space above the board and your hole cards sit under it). The table message (.table-msg) lives in that row too: centred under the board on tall screens (max 74vw), on phones in that narrow column where it wraps to 2–3 lines rather than being cut off by the right seat (Nic 2026-09-21). The old 'Pot $x' pill is gone; pot chips still gather under the board and fly to the winner.
- Blackjack totals (Nic, 2026-09-21, his hit-total.js): setTotalBadge() in blackjackTable.js — every hand total and the dealer's is a dark plate with a gold rim, serif; on change the old number slides up/fades and the new rises in (220 ms) with a 0.7 s glow; 21 = bright gold + '21' caption, bust = red plate, shake, 'BUST' caption. Badges are computed from the cards actually ON THE TABLE (querySelectorAll .pcard), not the engine's hand — Round.deal() deals everything at once while the UI animates card by card, so the engine total leaked early ('15' on a lone queen). Hidden until two cards are showing. Toasts now drop in under the top bar (house style) instead of over the action buttons. Nic (2026-09-21): no generic white-on-black text pills anywhere — .table-msg (hold'em) and .bj-msg (blackjack) are gold serif on the green-and-gold plate like everything else.
- Result banners (Nic, 2026-09-21, his blackjack-results.js): resultBanner(container, {type win|lose|bust|push, amount, title, caption, sub}) in components.js — caption / big serif title / amount line; rises in .38 s, fades over the last .35 s; 2.2 s total in a hand, 2.6 s for a whole game (Nic: 3.25 was too long); the win/lose sound plays WITH the banner, never after it (hold'em sets lossSoundPlayed so finishHand does not repeat it); win = green plate, gold halo, gleam sweep, 14 sparks; bust shakes; lose/push quiet. Used everywhere: blackjack (win unless BIG → old chip-stack win-banner; bust when every hand busted; Dealer wins; Push 'Bet returned'; compact per-hand tags WIN +$x / LOSE / BUST / PUSH in the same style), hold'em (win with the hand name as caption; lose only when you were in at showdown — '{names} win(s)' with their hand; folds show nothing), cribbage and farkle (game won / '{opp} wins' GAME OVER before the play-again modal; skunk / multi-opponent farkle wins keep the BIG WIN screen).
- Buttons (Nic, 2026-09-21, from his mockup): hold'em action bar, raise panel, Deal/next bar and the blackjack Hit/Stand bar are a gold-edged green tray with serif (Georgia) gold-rimmed buttons: Fold dark grey, Check/Call green (amount in gold), Bet/Raise gold. Farkle/cribbage keep their own action look. The "‹ Leave table" button on every table and "‹ Lobby" on the select screens use the same green-and-gold (`.btn.leave-btn` — needs the .btn specificity or .btn.ghost wins). All modals (.modal: buy-in/who's playing, Leave the table?, game over, settings, bust) and the table-select tiles are in the house scheme too: green gradient, 2px gold border, serif gold headings, ghost buttons dark green with gold rim, primary gold, danger red with gold rim.
- Ken's line is exactly "Ken gave you {amount}, go have fun." — no "Damn it", no "Bank Manager" label.
- Voice lines are `{ text, file }` pairs per line; recorded lines are preferred when present. The text bubble ALWAYS shows, even with a recording (Nic's rule).
- `playerWin` (you win a big pot) is only said by someone who was still in the hand at the end and lost chips — never by someone who folded (Nic caught Courtney cheering after folding). Courtney's playerWin no longer reuses her "Hell yeah!" (08); she uses the generic line until she records 15.
- Persona `chatty` (default 1) scales how often a character talks; `taunt` trigger = trash talk when you fold to them / bust / count nothing. Freddy is the trash-talker (chatty 1.8).
- Recording plan: one folder per person, files `NN-trigger.m4a` per the list in voice-file-names.md; Nic edits the wording and sends the list with the audio. Clips are cut/normalised with ffmpeg (loudnorm I=-16) and saved as MP3 — the artifact host does not serve .m4a. If one long recording comes in, split it by silence (energy envelope, gap >= 0.4 s) and match segments to the list in order. Files: `assets/voice/<id>/<file>`.
- Sounds (all Nic's recordings in assets/sfx): deal.mp3 per card, check.mp3 (two taps), call.mp3 (short "ante up" chip drop: only for limping in / calling the blind, and quietly when blinds are posted), raise.mp3 (bets, raises AND calling a raise — Nic's call), allin.mp3, dice.mp3, lose = his negative.mp3 pitched down; turn change/fold = synth tap.
- Music + room sound (audio.js, all WebAudio: element → lowpass → gain, because iPhone Safari ignores element.volume): songs in assets/music/song-1..25.mp3 — 1–13 are his dad's picks — Nic's setlist.json (2026-09-22) names them: 5 'Hug n a kiss' (Dad), 10 'Caleb on da mic' (Dad feat. Caleb), 11–13 Jackyl and Hide / Bad Company / House of the Rising Sun (Five Finger Death Punch); 1–4 and 6–9 are still 'Song N' by Dad (his own recordings), 14–25 added 2026-09-22: Finish What Ya Started, Hot for Teacher, Jump, Drop Dead Legs, Atomic Punk, Eruption, Runnin' with the Devil (VH), Little Wing, Pride and Joy (SRV), Stranglehold (Nugent), Rock Candy (Montrose), Poundcake (VH). New songs: `python3 tools/make_song.py <file> <N>` (the recipe: trim to the music onset, fade in .5 / out 1.5, highpass 80, lowpass 2200 p=2 + 4000 p=2, loudnorm I=-31, mono 64k), then a line in setlist.json ([{file,title,artist}]), then build_sw.py. music.init probes song-1..60 (stops at the first gap) and reads setlist.json for titles. Music Library (v93; was 'Setlist' — Nic: it's a list of songs on shuffle, not a setlist. Radio widget button says 'Songs', panel title 'Music Library' with a CASINO RADIO eyebrow, ✕ close, 'Now playing' badge on the current row, 'Changes save automatically'; lobby.js showSetlist() — function/CSS names still say setlist): every song with an on/off checkbox (settings.setlistOff = file names that are out of the shuffle; activeList() filters, the queue rebuilds when it changes; switching off the playing song skips; switching everything off leaves the floor quiet and switching one back on restarts) and a ▶ play-now button (music.playSong, also switches that song back on). Songs are NOT in the service-worker precache any more (tools/build_sw.py STREAM rule; they stream with range requests and are never cache.put) — 25 songs is ~38 MB, past what iOS reliably keeps in the Cache API, so offline launches have no music; everything else is cached (11.9 MB). Shuffled with a 3 s gap (SONG_GAP); lobby level = musicVolume setting (0.7), at a table 0.4× and lowpassed at 1400 Hz (DUCK, TABLE_MUFFLE). Lobby room sound = assets/music/lobby-room.mp3 (Nic's poker_room clip made into a seamless 50.8 s loop, -24 LUFS) at ROOM_LEVEL 0.4× music volume; plays only on the lobby floor (fades out on duck(true) — select screens and games), toggle 'Lobby room sound' (settings.roomSound). music.debug() shows the live graph state. music.skip() = next song with no gap. Casino Radio (2026-09-22, from Nic's mockup): the music left Settings — lobby footer now holds radioWidget() (lobby.js: '♪ CASINO RADIO ⌄' header → showRadio() modal with Music on/off, Volume, Lobby room sound, Skip, Setlist; body = now-playing title/artist, ❚❚/▶ pause (music.toggle(): ramps down then pauses, userPaused keeps the tap-retry from restarting it), ⏭ skip, ☰ Setlist; polls every 800 ms while mounted) beside a big '⚙ Settings' (settingsButton(), .btn.settings-btn, house style, stretches to the radio's height). v100: Casino Radio is a PAGE of the Settings panel (showSettings opts.page 'settings'|'radio'; showRadio() = page 'radio'): same box, no stacking; the radio page shows a Now-playing box (title/artist, 800 ms refresh), Music on/off, Volume, Pause/play, Skip, Music Library, and 'Done' (close) — plus '‹ Back' (to Settings) only when the radio page was reached through Settings (fromSettings); the pause button reads 'Pause' while playing and 'Play' otherwise (v101). Settings has sound/voices/Lobby room sound (moved here — ambience, not music)/speed/'♫ Casino Radio ›' link/art/save. Every table has a ♫ .note-btn (noteButton(root)) stacked ABOVE the gear at the right edge (v112 — side by side pushed the hold'em tray over the hole cards and the farkle bar under the buttons on iPhone; --gear is clamp(44px, 12.5vh, 60px), stack 12 px from the edges), the trays keep their original footprint (right = --gear + 24px) that opens the radio page directly ( the tables' gear is the SVG gear too (icon('gear')) and the notes icon's viewBox is shifted so its visible shape is centred; the hold'em tray/nextbar leave room for both: right = 2×--gear + 32px); the slots' painted SOUND button opens Casino Radio too; the tables' round .gear-btn was restyled bigger in the house style (48–60 px, gold gear). v95: bar + Settings restyled to Nic's second mockup — near-black green radial panel, 2 px bevelled gold frame, SVG gold icons (lobby.js icon(): pause/play/skip/list/gear/notes; gear path is Material's), cream serif text; radio-btn rings and the tables' gear share the bevel. v97: the bar has a fixed width (clamp(280px, 43vw, 480px)) so it never resizes between songs and the buttons stay anchored; between songs the title area is simply empty (Nic's mockup); a title wider than its area gets a slow back-and-forth marquee (both edges fade while scrolling — a mid-scroll title read as chopped) (.radio-title.scroll, --shift/--secs set in refresh(), fade mask on the right), shorter ones just sit; 'Music is off' / 'Quiet on the floor' / 'No songs' still show as text.
- Table art: assets/img/table/felt-holdem.png is Nic's oval poker table cut out onto transparency (from his 1774×887 render; surround removed with an ellipse mask). `.holdem .felt.has-art` draws it at 75%×73.3% centred at 50%/54% — exactly where the old drawn .rail sat — so the seats stay around the table on the dark floor (Nic: people must not sit on the felt). On phones (max-height 500px) it is 75%×66% at 50%/83%: lower and flatter so the top seat’s name plate just meets the rail instead of covering it. Blackjack (2026-09-21): Nic's clean render assets/img/table/felt-blackjack.jpg (1774×887, whole screen incl. room, nothing on the table — he re-rendered it empty after my inpainted version showed smears; Nic notices smudges, never ship visible inpainting) goes on `.bj.has-art` (the table-screen) stretched 100%/100%; SPOTS_ART in blackjackTable.js = the painted betting circles measured from the render (24.4/53.6, 50/63 [circle centre 59.8, chip sits a bit low], 76.1/54.3), portraits at 11%/89% bottom corners with their bubbles, companion plates under their circles, human plate on the felt at 38%/73% (the action bar would cover it below), dealer at top 9% in front of the tray, msg top-left. Betting panel on this table is styled after his mockup (`.bj.has-art .actionbar.betting`: one gold-edged green serif panel with 'Nic $250 / Bet $5', chips over Clear/Min/Max, gold Deal inside); the bet readout shows name+stack and bet on every table. Cards on this table are .93× card-h so yours clear the dealer's on a phone. Cribbage (2026-09-21): Nic's scene assets/img/table/felt-cribbage.jpg (1774×887, whole screen, board painted in) on `.crib.scene` (the table-screen); the board overlay (.cb-board.scene-board) covers the whole screen and pegs/plates/windows are placed by SCENE in cribbageTable.js — hole i at x = 20.575 + 1.0569·i %, rows y 15.22/18.04 (lane 0 out/back) and 21.42/24.24 (lane 1); 61 holes per row (0..60), start slot at i=-1; his peg PNGs at 2.1% width. Layout from his mockup: opponent + plate left, their face-down cards top centre under their name, COUNT badge centre, STARTER + crib stack at right 12% (in from the rail, per his 2nd mockup), labels STARTER / YOUR CRIB / opponent name in gold serif uppercase letter-spaced (#e6c36a), crib label above a fanned .cb-crib-stack, your hand bottom centre, your plate bottom-left, status line in a gold-edged tab at the bottom centre (the old dealer tag is hidden). Between hands a big gold Deal sits in the middle of the empty felt (waitDeal → .cb-deal-mid) instead of the corner. Points pops (.cb-points) say whose they are: 'YOUR POINTS / +8 / Hand' in gold with a glow, centred under the count panel; the opponent's in red beside their portrait. BUT Nic wants HIS board art (cribbage-board.png with the standing pegs, name plates left, score windows right) kept: on the scene it is laid over the painted board (`.cb-board.art.on-scene`: 82.6% wide at top 8.6%, centred) and the scene's own hole map (SCENE/buildSceneBoard) is only the fallback if that PNG is missing. Both pegs of each colour show from the start: in START HOLES drilled into cribbage-board.png (copies of hole 1 at x 9.85% on all four rows; no room for two in-line before hole 1): front peg in the top-row start hole, back peg in the one directly below it (holePos(lane,-1)); the bottom-row one doubles as the game hole (121). Nic: pegs must sit in real holes and never appear out of nowhere.
- Startup (Nic, 2026-09-21): phones won't play sound before a tap, so the app first tries video.play() unmuted on load: if the browser allows it (most desktops) the intro just runs; if it refuses (iPhone/iPad) the splash gets .gate and shows a pulsing "Tap to enter" on the intro's first frame (assets/img/splash.jpg); that tap starts Nic's 12 s intro video (assets/video/intro.mp4, 1536×768 h264 + aac, src directly on the <video> so errors reach us) WITH sound while boot() loads (~1 s). Logo forms at normal speed, then once loaded the rest runs at 2.2× (INTRO_LOGO_AT 5.2 / INTRO_FAST) → ~8 s total. 'Tap to skip' after 2.5 s. If nothing decodes within 4 s (or on error) the game just opens. #splash gets .ready when the tap listener is live, .started after the tap, .can-skip later — the smoke tests wait for .ready, tap, then tap again on .can-skip, and repeat after every page.reload().
- ffmpeg gotcha: put `-ss` BEFORE `-i` when trimming mp3s or you get silence.

## Where things live
- `js/app.js` routing · `js/core/` poker.js, evaluator.js, blackjack.js, cribbage.js, farkle.js, luck.js, ai.js, bank.js, audio.js, assets.js (art keys → file paths)
- `js/content/` characters.js, lines.js, tables.js, tiers.js, lobby.js (hotspot rectangles)
- `js/ui/` lobby.js, tableSelect.js (shared picker), holdemTable.js, blackjackTable.js, components.js, dom.js
- `css/app.css` everything · `README.md` art file names and sizes · `tools/` image prep scripts
