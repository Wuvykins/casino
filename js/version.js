// Build number shown at the foot of Settings so Nic can tell which build a phone has. Bump it with every zip;
// tools/build_sw.py reads it too. Temporary — comes out when the game is finished.
export const VERSION = 'v171';
